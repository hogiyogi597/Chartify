const Tabs = {
    parts: ['tabs', 'tab'],
    baseStyle: {
        tabs: {
        },
        tab: {
            _selected: { backgroundColor: 'brand.primary-light' },
            _focus: { boxShadow: 'none' }
        }
    }
}

export default Tabs